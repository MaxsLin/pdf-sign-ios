# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PDFSign' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PDFSign
  pod 'CryptoSwift', '~> 1.0'
  pod 'Zip', '~> 1.1'
  pod 'SnapKit', '~> 5.0.0'
  pod 'UberSignature', '~> 1.0'

  target 'PDFSignTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
